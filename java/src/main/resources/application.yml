# 服务器配置
server:
  port: 8080

# Spring应用配置
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  # MVC配置
  mvc:
    format:
      date-time: yyyy-MM-dd HH:mm:ss

  # 激活的环境配置，此处为开发环境
  profiles:
    active: dev

